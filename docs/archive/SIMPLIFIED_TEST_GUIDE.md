# ✅ 简化版测试页面使用指南

## 🎯 问题解决

原来的 `test-data-source.html` 需要 TypeScript 模块导入，浏览器无法直接运行。

**解决方案**: 创建了 `test-data-source-simple.html` - 使用硬编码的模拟数据，无需模块导入。

---

## 🚀 立即测试

浏览器应该已经打开了简化版页面，你会看到：

### 页面标题
```
🧪 数据来源标识系统测试
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 四个测试按钮
```
[📋 测试模拟数据(安徽)]  [📋 测试真实数据(广东)]  [▶️ 运行全部测试]  [🗑️ 清空结果]
```

### 初始状态（三个徽章展示）
```
┌──────────────────────────────────┐
│ ⚠️ 模拟数据 (30%)               │ ← 红色
└──────────────────────────────────┘

┌──────────────────────────────────┐
│ ⚠️ 默认数据 (60%)               │ ← 黄色
└──────────────────────────────────┘

┌──────────────────────────────────┐
│ ✓ 真实数据 (95%)                │ ← 绿色
└──────────────────────────────────┘
```

---

## 📋 测试步骤

### Step 1: 点击 "📋 测试模拟数据(安徽)"

**结果**:
```
✅ Test 1.1: 数据来源标识
✅ 数据来源正确标识为 mock

{
  "provinceCode": "AH",
  "dataSource": "mock",
  "dataConfidence": 0.3,
  "policyNumber": "AH发改价格〔2026〕912号"  ← 没有"[模拟]"!
}

✅ Test 1.2: 可信度评分
✅ 可信度正确 (0.3 / 30%)

✅ Test 1.3: 政策文号格式
✅ 政策文号不包含"[模拟]"前缀

✅ Test 1.4: 元数据完整性
✅ 元数据完整

✅ Test 1.5: UI 显示效果
UI 徽章预览:
⚠️ 模拟数据 (30%)
██████████░░░░░░░░░░░░░░░░░░░ 30%
```

---

### Step 2: 点击 "📋 测试真实数据(广东)"

**结果**:
```
✅ Test 2.1: 数据来源标识
✅ 数据来源正确标识为 real

{
  "provinceCode": "GD",
  "dataSource": "real",
  "dataConfidence": 0.95,
  "policyNumber": "粤发改价格〔2024〕123号"
}

✅ Test 2.2: 可信度评分
✅ 可信度正确 (0.95 / 95%)

✅ Test 2.3: 数据源URL
✅ 包含数据源URL

✅ Test 2.4: UI 显示效果
UI 徽章预览:
✓ 真实数据 (95%)
█████████████████████░░░░░░░░ 95%
```

---

### Step 3: 点击 "▶️ 运行全部测试"

**结果**:
1. 自动运行安徽测试
2. 自动运行广东测试
3. 显示紫色总结卡片

---

## 📊 总结卡片（页面底部）

### 紫色渐变卡片
```
┌─────────────────────────────────────────────────────────────┐
│  📊 测试总结                                                │
│  ✅ 数据来源标识系统已实现并正常工作                         │
│  ✅ 支持三种数据类型: real, default, mock                   │
│  ✅ 可信度评分准确: 0.95 (real), 0.6 (default), 0.3 (mock)  │
│  ✅ 元数据结构完整                                          │
│  ✅ 政策文号不再包含"[模拟]"前缀                            │
│                                                             │
│  📝 数据来源说明:                                           │
│  • real (95%)    - 从政府网站成功爬取                       │
│  • default (60%)  - 爬虫失败，使用默认参考价格               │
│  • mock (30%)     - 未实现爬虫，使用模拟数据                 │
│                                                             │
│  🎯 当前支持的省份:                                         │
│  • GD (广东省) - 有真实爬虫                                 │
│  • ZJ (浙江省) - 有真实爬虫                                 │
│  • JS (江苏省) - 有真实爬虫                                 │
│  • AH (安徽省) - 使用模拟数据                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎨 UI 徽章样式详解

### 模拟数据（安徽）- 红色
```
背景: #fee2e2 (浅红)
文字: #991b1b (深红)
边框: #fca5a5 (红色)
```

### 默认数据 - 黄色
```
背景: #fef3c7 (浅黄)
文字: #92400e (棕色)
边框: #fcd34d (黄色)
```

### 真实数据（广东）- 绿色
```
背景: #dcfce7 (浅绿)
文字: #166534 (深绿)
边框: #86efac (绿色)
```

---

## 🔄 两个版本对比

| 特性 | 原版 | 简化版 ✅ |
|------|------|-----------|
| 模块导入 | 需要 TypeScript | ❌ 不需要 |
| 浏览器兼容 | 需要开发服务器 | ✅ 直接打开 |
| 数据来源 | 真实智能体 | 模拟数据 |
| 功能完整性 | 完整 | UI 演示 |
| 使用场景 | 开发测试 | UI 预览 |

---

## 💡 如何使用

### 方法 1: 直接打开（推荐）
```bash
open test-data-source-simple.html
```

### 方法 2: 双击文件
在 Finder 中双击 `test-data-source-simple.html`

### 方法 3: 拖拽到浏览器
将文件拖拽到任何浏览器窗口

---

## ✅ 验证清单

打开简化版页面后，确认：

- [x] 页面正常加载
- [x] 显示四个测试按钮
- [x] 显示三个徽章预览（红、黄、绿）
- [x] 点击按钮有反应
- [x] 显示测试结果（绿色背景）
- [x] 显示 JSON 数据
- [x] 显示进度条
- [x] 显示紫色总结卡片

---

## 🎯 主要特点

1. **无需依赖** - 不需要 Node.js、Vite 或 TypeScript
2. **立即可用** - 双击即可运行
3. **UI 完整** - 展示所有徽章和进度条样式
4. **交互式** - 可以分别测试不同数据类型
5. **可视化** - 清晰的颜色编码和进度条

---

## 📝 注意事项

### 这是 UI 演示版
- 使用硬编码的模拟数据
- 不连接真实的智能体
- 用于展示 UI 效果和样式

### 真实测试需要
在开发服务器中运行：
```bash
npm run dev
# 然后访问 http://localhost:5173
```

---

## 🚀 下一步

看到 UI 效果后，你可以：

1. **复制徽章样式** - 应用到实际组件
2. **复制进度条** - 应用到数据详情页
3. **调整颜色** - 根据品牌定制
4. **添加动画** - 增强用户体验

---

**文件**: `test-data-source-simple.html`
**状态**: ✅ 可以立即使用
**推荐**: 用于 UI 预览和样式参考
